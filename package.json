{
  "name": "vitrineverse",
  "version": "1.0.0",
  "private": true,
  "description": "Jeu cosy de collection et de mise en scène 2D.",
  "scripts": {
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "node --test tests/static-contracts.test.js",
    "test:e2e": "playwright test",
    "build": "node build_single_file.js",
    "serve": "node scripts/static-server.js",
    "qa": "npm run build && npm run test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  },
  "engines": {
    "node": "24.x"
  }
}
